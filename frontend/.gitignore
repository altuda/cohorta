# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is committed on purpose: the server is deployed via git clone/pull and
# cannot build the frontend. Rebuild with `ONCOPLOT_BASE_PATH=/oncoplot/ npm run build`
# before committing. (dist intentionally NOT ignored.)
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
